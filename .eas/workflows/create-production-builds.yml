name: Create Production Builds

# main branch'ine her push/merge'de otomatik tetiklenir
on:
  push:
    branches: ['main']

jobs:
  build_android:
    type: build
    params:
      platform: android
      profile: production

  # iOS job: Apple Developer hesabı + credentials hazır olduğunda yorumu kaldır
  # build_ios:
  #   type: build
  #   params:
  #     platform: ios
  #     profile: production
